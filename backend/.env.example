# Backend-Umgebung (Beispiel — kopieren nach `.env` und Werte setzen)

# PostgreSQL (z. B. Supabase „Connection string“ / URI)
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# Produktiv: langen zufälligen Wert setzen (nicht committen)
# DJANGO_SECRET_KEY=
